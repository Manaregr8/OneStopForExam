import { makeGenerateMetadata, makeSectionPage } from "../_shared";

export const generateMetadata = makeGenerateMetadata(
  "previous-year-question-papers"
);
export default makeSectionPage("previous-year-question-papers");
